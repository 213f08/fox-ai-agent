# Spring AI 实战：打造带记忆的智能客服（ChatClient + ChatMemory + Advisor）

> **CSDN 发布信息**（复制用）：  
> 摘要：从单轮问答到"记住上下文"的多轮对话。拆解 ChatClient 组装、MessageChatMemoryAdvisor 记忆机制、会话隔离（chatId），并分享把记忆落盘为文件的实现思路——应用重启后历史不丢。  
> 标签：Spring AI、ChatMemory、多轮对话、Java、AI应用  
> 分类：AI 应用开发 / Spring AI



---

## 一、需求：为什么对话必须"有记忆"

先看两个现象：

**无记忆的对话**：

```
我：我叫小明，我的会员卡快过期了，能续费吗？
AI：可以的，您需要续费会员卡。（……下一条消息……）
我：那怎么续？
AI：请问您是谁？之前聊过什么吗？
```

**有记忆的对话**：

```
我：我叫小明，我的会员卡快过期了，能续费吗？
AI：可以的，您需要续费会员卡。
我：那怎么续？
AI：小明您好，会员续费在 App「我的-会员中心」即可操作，支持微信/支付宝支付。
```

区别在哪？**后者知道"我"是谁、知道上文说过"会员卡续费"**——这就是 ChatMemory（对话记忆）的价值。

> 大模型本身是"无状态"的：每次调用都是独立的，它不记得上一轮聊过什么。所谓多轮对话，本质是**每次请求都把历史消息一起发给模型**，让它"看起来记得"。这个"历史存哪、怎么取、怎么更新"的问题，就是 ChatMemory 要解决的。

---

## 二、第一版：ChatClient 单轮问答

Spring AI 里，一次对话的最小单位是 `ChatClient`：

```java
@Component
public class ChatAssistant {

    private final ChatClient chatClient;

    // dashscopeModel 由 spring-ai-alibaba-starter-dashscope 自动装配
    public ChatAssistant(ChatModel dashscopeModel) {
        this.chatClient = ChatClient.builder(dashscopeModel)
                .defaultSystem("你是专业、耐心的智能客服助手")
                .build();
    }

    public String chat(String message) {
        return chatClient.prompt()
                .user(message)
                .call()
                .content();      // 拿纯文本回复
    }
}
```

调用：

```java
assistant.chat("我的会员卡能续费吗？");
// → "可以的，您需要续费会员卡"
```

**问题**：如果你接着问"那怎么续？"，模型完全不知道上文。因为第二次调用时，历史消息根本没发给它。

---

## 三、升级：Advisor 链 + ChatMemory

Spring AI 的解法是 **Advisor（顾问）机制**——可以把它理解成"请求拦截器"：

```
用户提问
   │
   ▼
┌──────────────────────────────────────────┐
│  Advisor 链（按顺序执行）                  │
│  ① MessageChatMemoryAdvisor              │
│     · 请求进来前：从记忆里捞历史 → 拼进 prompt │
│     · 回复生成后：把这一轮对话写回记忆         │
│  ② MyLoggerAdvisor（自定义日志）            │
│     · 打印每轮请求/回复，方便调试            │
└──────────────────────────────────────────┘
   │
   ▼
   ChatModel（通义千问 qwen-plus）
```

用代码把记忆挂上去：

```java
@Component
public class ChatAssistant {

    private final ChatClient chatClient;

    public ChatAssistant(ChatModel dashscopeModel) {
        // ① 准备一块"记忆"：最多保留最近 10 条消息
        ChatMemory memory = MessageWindowChatMemory.builder()
                .chatMemoryRepository(new InMemoryChatMemoryRepository())
                .maxMessages(10)
                .build();

        // ② 把记忆装进 Advisor，随 ChatClient 一起生效
        this.chatClient = ChatClient.builder(dashscopeModel)
                .defaultSystem("你是专业、耐心的智能客服助手")
                .defaultAdvisors(
                        MessageChatMemoryAdvisor.builder(memory).build(),
                        new MyLoggerAdvisor()     // 自定义 Advisor，见第四节
                )
                .build();
    }

    public String chat(String message, String chatId) {
        return chatClient.prompt()
                .user(message)
                // ③ 告诉框架"这是哪个会话的对话"
                .advisors(spec -> spec.param(ChatMemory.CONVERSATION_ID, chatId))
                .call()
                .content();
    }
}
```

只有 3 处改动：

1. 造了一个 `ChatMemory`（窗口大小 10）
2. 用 `MessageChatMemoryAdvisor` 包住它，注册进 defaultAdvisors
3. 每次对话传一个 `chatId`

**此时再测**：

```
chat("我的会员卡能续费吗？", "session-001")   → "可以的，您需要续费会员卡"
chat("那怎么续？",            "session-001")   → "会员续费在 App「我的-会员中心」即可操作……"
```

第二个问题它"记得"上文了！

---

## 四、chatId：会话隔离是怎么实现的

注意上面的调用多了一个 `chatId`。它的作用是把"记忆"按会话切分：

- 同一个 `chatId` → 共享同一份历史 → 像同一个人连续聊天
- 不同 `chatId` → 各自独立，互不串线 → 像两个用户同时在线

```
session-001 的历史：["我叫小明…", "我会员卡过期…", "那怎么续？"]
session-002 的历史：["我要投诉物流太慢！"]
```

这对客服系统是**刚需**——不同顾客同时咨询，绝不能互相看到对方的消息。实现上，`ChatMemory.get(chatId)` 按 key 取历史，天然支持多会话。

> 💡 一句话记忆：**chatId 就是对话的"主键"**。传入同一个 id 就是续聊，换个 id 就是新会话。

---

## 五、进阶：把记忆"落盘"成文件（重启不丢）

上面的内存版 `InMemoryChatMemoryRepository` 有个致命问题——**应用一重启，记忆全丢**。

生产上可以换 Redis，但为了理解机制，我选择自己实现一个 `ChatMemory` 接口，把历史序列化到本地文件（Kryo 高性能序列化）：

```java
public class FileChatMemory implements ChatMemory {

    private final String baseDir;
    private final int maxMessages;
    private static final Kryo kryo = new Kryo();

    static {
        kryo.setRegistrationRequired(false);
        kryo.setInstantiatorStrategy(new StdInstantiatorStrategy());
    }

    public FileChatMemory(String dir, int maxMessages) {
        this.baseDir = dir;
        this.maxMessages = maxMessages;
        File basedir = new File(dir);
        // 注意：用 mkdirs() 而不是 mkdir()，否则 tmp/ 这类不存在的父目录会创建失败
        if (!basedir.exists() && !basedir.mkdirs()) {
            throw new IllegalStateException("无法创建记忆存储目录: " + basedir.getAbsolutePath());
        }
    }

    @Override
    public void add(String conversationId, Message message) {
        save(conversationId, List.of(message));
    }

    @Override
    public void add(String conversationId, List<Message> messages) {
        save(conversationId, messages);
    }

    @Override
    public List<Message> get(String conversationId) {
        File file = fileOf(conversationId);
        if (!file.exists()) return new ArrayList<>();
        try (Input in = new Input(new FileInputStream(file))) {
            return kryo.readObject(in, ArrayList.class);
        } catch (IOException e) {
            throw new RuntimeException("读取会话记忆失败", e);
        }
    }

    @Override
    public void clear(String conversationId) {
        File file = fileOf(conversationId);
        if (file.exists()) file.delete();
    }

    /** 每个会话一个 .kryo 文件，天然按 chatId 隔离 */
    private void save(String conversationId, List<Message> messages) {
        File file = fileOf(conversationId);
        // ★ 先读已有历史再合并——否则会覆盖掉之前的对话！
        List<Message> all = get(conversationId);
        all.addAll(messages);

        // 滑动窗口：超出 maxMessages 裁掉最早的消息，防文件无限膨胀
        if (all.size() > maxMessages) {
            all = new ArrayList<>(all.subList(all.size() - maxMessages, all.size()));
        }

        try (Output out = new Output(new FileOutputStream(file))) {
            kryo.writeObject(out, all);
        } catch (IOException e) {
            throw new RuntimeException("写入会话记忆失败", e);
        }
    }

    private File fileOf(String conversationId) {
        return new File(baseDir, conversationId + ".kryo");
    }
}
```

接入方式只需换一行——把原来的 `MessageWindowChatMemory` 换成我们的 `FileChatMemory`：

```java
ChatMemory memory = new FileChatMemory("./data/chat-memory", 10);
```

> 完全兼容：`ChatMemory` 是 Spring AI 的标准接口，任何实现都能直接塞进 `MessageChatMemoryAdvisor`。

**重启验证**：跑一次对话 → 杀掉进程 → 重启 → 继续用同一个 chatId 提问，模型依然记得之前聊的内容。目录下会出现 `session-001.kryo`、`session-002.kryo`，每个文件对应一个会话。

---

## 六、我踩过的坑（真实 Bug 复盘）

### Bug 1：覆盖写——"聊着聊着，AI 失忆了"

第一版 `save()` 是这么写的：

```java
// ❌ 错误版本
private void save(String conversationId, List<Message> messages) {
    try (Output out = new Output(new FileOutputStream(file))) {
        kryo.writeObject(out, messages);   // 只写"新来的几条"
    }
}
```

**后果**：每轮对话都只把"当前这 1 条"写进文件，**把之前的所有历史覆盖掉了**。第二轮的 AI 只能看到上一轮的 1 条消息，第三轮更惨——历史越聊越短，表现为"AI 逐渐失忆"。

**修复**：先 `get()` 读出完整历史 → 合并新消息 → 整体落盘（上文第五节就是修复版）。

```
❌ writeObject(新消息)                    ✅ writeObject(旧历史 + 新消息)
```

> 这是我自己踩过的真实 bug：加了记忆功能后，测第 1 轮正常、第 2 轮正常、第 3 轮突然"失忆"，排查半天发现是文件被覆盖了。

### Bug 2：目录创建用错方法

```java
// ❌ basedir.mkdir()   —— 如果父目录 tmp/ 不存在，静默失败（不报错也不创建）
// ✅ basedir.mkdirs()  —— 连带创建所有不存在的父目录
```

`mkdir()` 失败是**不抛异常**的，后面写文件时才会报 `FileNotFoundException`，非常误导排查。

### Bug 3：序列化类没注册

Kryo 默认 `registrationRequired=false` 即可工作；但如果项目里开了 `true`（严格注册模式），所有序列化类都要提前注册，否则抛 `KryoException`。本地学习建议保持默认宽松模式，等真正要上生产再考虑注册优化。

---

## 七、易错点速查表

| 坑                         | 原因                            | 正确做法                         |
| ------------------------- | ----------------------------- | ---------------------------- |
| 多轮对话"不记得"上文               | 没挂 `MessageChatMemoryAdvisor` | defaultAdvisors 注册记忆 Advisor |
| 重启后记忆丢失                   | 用了内存版 Repository              | 换文件 / Redis 持久化实现            |
| 历史越聊越短 / 覆盖写              | save 时没先读历史就覆盖                | 先 get 合并再整体写回                |
| 不同用户互相串线                  | 所有对话共用同一个 chatId              | 每个用户/会话单独一个 chatId           |
| 记忆无限膨胀                    | 没限制窗口大小                       | `maxMessages(10)` 滑动窗口裁剪     |
| 目录报 FileNotFoundException | `mkdir()` 父目录不存在              | 用 `mkdirs()`                 |
| 记忆里全是旧消息，新问题答偏            | 窗口太小把关键上下文挤掉了                 | 按场景调窗口：客服 10~20 条够用          |

---

## 八、面试怎么讲

被问到"你项目里的多轮对话怎么实现"：

> 我基于 Spring AI 的 ChatClient + MessageChatMemoryAdvisor 实现多轮对话：Advisor 会在每次请求前从 ChatMemory 按 chatId 取出该会话的历史消息拼进 prompt，回复后再写回。记忆默认是内存版，但重启会丢，所以我实现了 ChatMemory 接口，用 Kryo 把每个会话的历史序列化到独立文件，做到重启不丢、会话隔离（每会话一个 .kryo），并用滑动窗口限制在最近 10 条控制 token 成本。

**加分追问预备**：

- "为什么不直接用 Redis？" → "学习阶段为了理解接口机制，先自己实现了文件版；生产可直接换 Spring 自带的 Redis ChatMemory，接口不变只换实现。"
- "窗口开多大？" → "10~20 条，兼顾上下文完整度和 token 成本（历史每次都要重发给模型）。"

---

## 九、下一步预告

记忆解决了"记得聊过什么"，但客服还答不了业务问题——"退货几天？""发票怎么开？"模型照样瞎编。下一篇讲 **RAG（检索增强）**：给客服接上企业知识库，让回答有据可依。

欢迎留言聊聊：你的客服/对话系统，记忆存在哪？（内存 / 文件 / Redis / 数据库？）
