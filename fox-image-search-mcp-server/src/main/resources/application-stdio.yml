# stdio 传输 profile：通过标准输入/输出与 MCP Client 通信
spring:
  ai:
    mcp:
      server:
        name: fox-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        stdio: true
  main:
    # stdio 模式不需要 Web 容器
    web-application-type: none
    banner-mode: off
